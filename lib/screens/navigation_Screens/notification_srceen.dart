import 'package:flutter/material.dart';

class NotificationSrceen extends StatelessWidget {
  const NotificationSrceen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Notifications Screen '),
    );
  }
}
